import { createAPIFileRoute } from '@tanstack/react-start/api'
import { api } from '@netko/{{ name }}-eden-treaty'

export const APIRoute = createAPIFileRoute('/api/$')({
  GET: ({ request }) => api.handle(request),
  POST: ({ request }) => api.handle(request),
  PUT: ({ request }) => api.handle(request),
  PATCH: ({ request }) => api.handle(request),
  DELETE: ({ request }) => api.handle(request),
})
